# Build output (regenerated by `hugo` / `hugo server`)
/public/
/resources/
.hugo_build.lock

# Local Hugo binary — keep it locally to run the site, don't commit it
hugo.exe
hugo

# R session artifacts
.Rproj.user
.Rhistory
.RData
.Ruserdata

# Python
__pycache__/
*.pyc

# OS junk
.DS_Store
Thumbs.db

# Local working notes / tool configs (not part of the site)
researchtopics.md
opencode.jsonc
.venv/

# Claude Code local launch config
.claude/
